### Unikraft development environment with nix and direnv
- Install nix and direnv
- `direnv allow`
- Manually export `UK_KRAFT_GITHUB_TOKEN` if you want to use kraft
    - `export UK_KRAFT_GITHUB_TOKEN=XXXXXXX`
